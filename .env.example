# ─── DocVision: Azure AI Foundry Credentials ─────────────────────────────────
# Copy this file to .env and fill in your values.
# These are automatically loaded by AzureConfig when keys are not set in YAML.
# .env is gitignored and will NOT be committed to source control.

# Azure Document Intelligence (Cognitive Services)
# Replaces: YOLO layout, CRAFT text detection, TrOCR, Table Transformer, Tesseract
AZURE_DOC_INTELLIGENCE_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/
AZURE_DOC_INTELLIGENCE_KEY=your-doc-intelligence-key-here

# Azure OpenAI (GPT Vision)
# Replaces: Donut KIE, LayoutLMv3 KIE
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_KEY=your-openai-key-here

# ─── Available Azure OpenAI Deployments ──────────────────────────────────────
# Set the deployment name that matches your Azure Foundry model deployment:
#   gpt-5.2       — Latest flagship model
#   gpt-5-nano    — Smallest / fastest / cheapest
#   gpt-5-mini    — Balanced speed & quality
#   gpt-4o-mini   — Previous-gen lightweight
#   gpt-4.1-mini  — Previous-gen lightweight variant
# AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
